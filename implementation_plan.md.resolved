# 室友生活協調系統 — 實作計畫

## 專案目標

為大學生合租族群與宿舍住宿生打造一套公平、清晰且現代化的生活協調工具，涵蓋三大核心功能模組。

---

## 技術架構

- **Frontend**: HTML5 + Vanilla CSS + JavaScript (Single Page App)
- **Storage**: LocalStorage（無後端，開箱即用）
- **設計風格**: 深色玻璃擬態（Glassmorphism）+ 漸層色彩 + 微動畫

---

## 功能模組規劃

### 模組一：公共費用拆帳 💰

- 新增支出紀錄（誰付、項目、金額、分攤對象）
- 自動計算每人應付 / 應收金額
- 代墊債務追蹤，顯示「誰欠誰多少」
- 一鍵結清功能，記錄還款歷史

### 模組二：值日生輪替排班 🧹

- 新增室友名單
- 設定清潔任務類型（掃地、拖地、倒垃圾…）
- 自動輪替演算法，支援手動換班申請
- 日曆式視覺呈現，顯示本週 / 下週排班

### 模組三：靜音協議模式 🌙

- 每位室友設定當前狀態（睡眠、讀書、休息、外出、開放聊天）
- 顯示預計可打擾時間
- 設置「請勿打擾」區間提醒
- 狀態燈號（色彩指示器）即時顯示

---

## 頁面結構

```
index.html          ← 主應用程式（SPA 單頁）
styles/
  main.css          ← 全域設計系統與動畫
js/
  app.js            ← 路由 / 狀態管理
  expenses.js       ← 拆帳模組邏輯
  roster.js         ← 排班模組邏輯
  silent.js         ← 靜音協議模組邏輯
  storage.js        ← LocalStorage 封裝
```

---

## 設計系統

- **主色調**: 深紫藍漸層 (`#0f0c29 → #302b63 → #24243e`)
- **強調色**: 霓虹紫 `#a855f7`、青綠 `#06b6d4`、暖金 `#f59e0b`
- **字體**: Google Fonts — Inter
- **卡片**: 半透明毛玻璃效果（backdrop-filter: blur）
- **動畫**: 頁面切換滑入、狀態燈號呼吸燈、卡片懸停浮起

---

## 驗證計畫

1. 在瀏覽器中完整操作三大模組
2. 確認 LocalStorage 資料持久化正常
3. 響應式版面（桌機 + 手機）驗證
